<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      metal:use-macro="api.macro('kotti:templates/view/master.pt')">

<tal:block metal:fill-slot="content">
<div class="idea-edit-page" style="max-width: 800px; margin: 0 auto; padding: 24px;">
    <h1>Edit Idea</h1>

    <form method="POST" class="form-horizontal">
        <input type="hidden" name="csrf_token" value="${python: request.session.get('csrf_token', '')}"/>
        
        <div class="panel panel-default" style="margin-bottom: 20px; padding: 20px; background: white; border-radius: 8px;">
            <div class="form-group" style="margin-bottom: 15px;">
                <label style="display: block; font-weight: bold; margin-bottom: 5px;">Title</label>
                <input type="text" name="title" value="${context/title}" class="form-control" 
                       style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;" required/>
            </div>

            <div class="form-group" style="margin-bottom: 15px;">
                <label style="display: block; font-weight: bold; margin-bottom: 5px;">Description</label>
                <textarea name="description" rows="6" class="form-control" 
                          style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;"
                          tal:content="context/description">Description</textarea>
            </div>

            <div class="row" style="display: flex; gap: 20px; margin-bottom: 15px;">
                <div style="flex: 1;">
                    <label style="display: block; font-weight: bold; margin-bottom: 5px;">Category</label>
                    <select name="category" class="form-control" style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;">
                        <tal:block tal:repeat="cat categories">
                            <option tal:attributes="value cat; selected python: 'selected' if cat == context.category else None"
                                    tal:content="python: categories[cat]">Category</option>
                        </tal:block>
                    </select>
                </div>
                <div style="flex: 1;">
                    <label style="display: block; font-weight: bold; margin-bottom: 5px;">Difficulty</label>
                    <select name="difficulty" class="form-control" style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;">
                        <tal:block tal:repeat="diff difficulties">
                            <option tal:attributes="value diff; selected python: 'selected' if diff == context.difficulty else None"
                                    tal:content="python: difficulties[diff]">Difficulty</option>
                        </tal:block>
                    </select>
                </div>
                <div style="flex: 1;">
                    <label style="display: block; font-weight: bold; margin-bottom: 5px;">Status</label>
                    <select name="status" class="form-control" style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;">
                        <tal:block tal:repeat="st statuses">
                            <option tal:attributes="value st; selected python: 'selected' if st == context.status else None"
                                    tal:content="python: statuses[st]">Status</option>
                        </tal:block>
                    </select>
                </div>
            </div>

            <div class="form-group" style="margin-bottom: 15px;">
                <label style="display: block; font-weight: bold; margin-bottom: 5px;">Needed Resources</label>
                <textarea name="needed_resources" rows="3" class="form-control" 
                          style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;"
                          tal:content="context/needed_resources">Resources</textarea>
            </div>

            <div class="form-group" style="margin-bottom: 15px;">
                <label style="display: block; font-weight: bold; margin-bottom: 5px;">Expected Outcome</label>
                <textarea name="expected_outcome" rows="3" class="form-control" 
                          style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;"
                          tal:content="context/expected_outcome">Outcome</textarea>
            </div>

            <div class="form-group" style="margin-bottom: 15px;">
                <label style="display: block; font-weight: bold; margin-bottom: 5px;">Tags (comma-separated, max 10)</label>
                <input type="text" name="tags" value="${python: ', '.join(context.tags or [])}" class="form-control" 
                       style="width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 4px;"
                       placeholder="AI, machine learning, python"/>
            </div>
        </div>

        <div style="display: flex; gap: 10px;">
            <button type="submit" class="btn btn-primary" style="background: #667eea; color: white; padding: 10px 20px; border: none; border-radius: 4px; cursor: pointer;">
                Save Changes
            </button>
            <a tal:attributes="href python: request.resource_url(context)" class="btn btn-default" 
               style="background: #6c757d; color: white; padding: 10px 20px; border-radius: 4px; text-decoration: none;">
                Cancel
            </a>
        </div>
    </form>
</div>
</tal:block>

</html>
